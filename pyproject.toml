[project]
name = "streamlit-nav"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rt7979", email = "zz9771@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "seaborn>=0.13.2",
    "streamlit>=1.63.0",
    "yfinance>=1.7.0",
]

[project.scripts]
streamlit-nav = "streamlit_nav:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
